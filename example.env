# Ollama Speedrun Configuration
# Copy to .env and modify as needed: cp example.env .env

# --- Network Discovery ---

# Ollama API port (default: 11434)
# OLLAMA_PORT=11434

# Timeout (ms) for probing remote hosts on subnet scan (default: 500)
# CONNECT_TIMEOUT_MS=500

# Timeout (ms) for localhost Ollama check -- more generous for cold starts (default: 5000)
# LOCALHOST_TIMEOUT_MS=5000

# Number of retries for localhost before giving up (default: 3)
# LOCALHOST_RETRIES=3

# Parallel batch size for subnet scanning (default: 50)
# SUBNET_BATCH_SIZE=50

# --- Enumeration ---

# Timeout (ms) for API calls to enumerate models (default: 10000)
# ENUM_TIMEOUT_MS=10000

# --- Scoring Weights ---
# Speed vs quality tradeoff for composite score. Must sum to 1.0.

# Weight for tokens/second throughput (default: 0.6)
# SPEED_WEIGHT=0.6

# Weight for response quality/length (default: 0.4)
# QUALITY_WEIGHT=0.4
